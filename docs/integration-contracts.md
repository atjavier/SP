# Tool And Feature Integration Contracts

## Purpose

This document is the single source of truth for how integrated tools/features work in SP:
- where each integration runs
- required inputs
- request/command format
- output structure (artifacts + DB/API shape)
- failure/diagnostic behavior

## Required Update Policy

For every newly integrated tool or major feature:
1. Add or update a section in this file.
2. Include exact request/command format and required params.
3. Include concrete input and output structure.
4. Link the related story key(s).

Use `docs/templates/integration-entry-template.md` for new entries.

---

## Installed Dependencies And Runtime Assets

This section records non-code dependencies/files installed for tool integrations and why they matter.

### A) Container-level runtime dependencies (Docker image)

Installed in `Dockerfile` via `apt-get`:
- `perl`, `git`, `curl`, `unzip`, `tabix`, `default-jre-headless`
- Perl/runtime libs required by VEP ecosystem:
  - `libdbi-perl`, `libdbd-mysql-perl`
  - `libwww-perl`, `libjson-perl`, `libhtml-tableextract-perl`
  - `liblist-moreutils-perl`, `libmodule-build-perl`
  - `libperlio-gzip-perl`, `libset-intervaltree-perl`
- Build/system libs for HTS/Bio::DB::HTS:
  - `build-essential`, `zlib1g-dev`, `libbz2-dev`, `liblzma-dev`

Significance:
- Enables VEP installation/runtime checks, HTS/tabix support, and SnpEff Java execution.
- Prevents historical runtime failures (`Bio::DB::HTS::Tabix`, missing Perl modules, missing `tabix`).

### B) VEP runtime assets (installed by `scripts/setup_vep_runtime.sh`)

Persistent location:
- Docker volume `vep-data` mounted at `/opt/vep`

Installed assets:
1. Ensembl VEP source/runtime
- Path: `/opt/vep/ensembl-vep`
- Installed from: GitHub branch `release/115`
- Significance: provides `vep` executable and Ensembl API modules used by classification/prediction.

2. VEP cache (human, GRCh38)
- Path: `/opt/vep/.vep/homo_sapiens/115_GRCh38`
- Installed via `INSTALL.pl --AUTO c`
- Significance: required for offline VEP annotation/classification/prediction.

3. AlphaMissense plugin module
- Path: `/opt/vep/.vep/Plugins/AlphaMissense.pm`
- Installed via `INSTALL.pl --AUTO p` (plugin install path)
- Significance: enables AlphaMissense annotation fields in VEP output.

4. AlphaMissense data file
- Path: `/opt/vep/.vep/Plugins/AlphaMissense_hg38.tsv.gz`
- Source URL default: `https://storage.googleapis.com/dm_alphamissense/AlphaMissense_hg38.tsv.gz`
- Significance: actual precomputed AlphaMissense scores.

5. AlphaMissense tabix index
- Path: `/opt/vep/.vep/Plugins/AlphaMissense_hg38.tsv.gz.tbi`
- Generated by: `tabix -s 1 -b 2 -e 2 -S 1`
- Significance: required for fast region lookups by plugin; missing index causes runtime failure.

6. VEP/Perl runtime readiness marker
- Path: `/opt/vep/.vep-ready`
- Significance: allows idempotent bootstrap (`Existing VEP runtime detected`).

### C) SnpEff runtime assets (installed by `scripts/setup_snpeff_runtime.sh`)

Persistent location:
- Docker volume `snpeff-data` mounted at `/opt/snpeff`

Installed assets:
1. SnpEff distribution zip (download artifact)
- Path: `/opt/snpeff/snpEff_latest_core.zip`
- Primary URL: `https://snpeff.blob.core.windows.net/versions/snpEff_latest_core.zip`
- Fallback URL: SourceForge mirror
- Significance: installer archive for `snpEff.jar`.

2. SnpEff runtime jar
- Path: `/opt/snpeff/snpEff/snpEff.jar`
- Significance: executable annotation engine used by annotation stage.

3. SnpEff genome DB (GRCh38.86)
- Path: `/opt/snpeff/snpEff/data/GRCh38.86/snpEffectPredictor.bin`
- Installed via: `java -jar snpEff.jar download -v -dataDir ./data GRCh38.86`
- Significance: required local reference DB for SnpEff annotations.

4. SnpEff readiness marker
- Path: `/opt/snpeff/.snpeff-ready`
- Significance: idempotent init marker to skip reinstall when complete.

### D) App data persistence assets

- Docker volume `instance-data` mounted at `/app/instance`
- Stores:
  - SQLite DB (`/app/instance/sp.db`)
  - run artifacts (`/app/instance/artifacts/<run_id>/...`)

Significance:
- Keeps run records/results durable across container restarts.

### E) Evidence local database assets

Installed by: `scripts/setup_evidence_runtime.sh` (Docker service: `sp-evidence-init`)  
Persistent location: Docker volume `evidence-data` mounted at `/opt/evidence`

Assets:
- dbSNP local VCF + tabix index
  - default path: `/opt/evidence/dbsnp/dbsnp_all_grch38.vcf.gz`
- ClinVar local VCF + tabix index
  - default path: `/opt/evidence/clinvar/clinvar_grch38.vcf.gz`

Significance:
- Enables local dbSNP/ClinVar retrieval when running in `offline`/`hybrid` mode.
- Supports reduced network dependency for pilot/tester runs.
- Runtime mode is controlled by `SP_EVIDENCE_MODE=online|offline|hybrid`.
- Project decision: gnomAD local dataset bootstrap is disabled due size constraints; gnomAD is run online for this SP.
- Default: gnomAD local bootstrap is disabled due to size; gnomAD runs online.
- Optional: enable local gnomAD by setting `INSTALL_GNOMAD=1` in `docker-compose` (sp-evidence-init) and providing local VCFs.
- Recommended local dataset: gnomAD v4.0 exomes sites VCFs (per-chrom files), stored under `/opt/evidence/gnomad/v4.0/exomes`.

---

## Integration Matrix

| Story Key(s) | Stage | Integration | Status |
|---|---|---|---|
| 2.2, 2.3 | parser | VCF parse + SNV filtering | Integrated |
| 4.1 | pre_annotation | local derived context | Integrated |
| 4.2 | classification | VEP consequence classification | Integrated |
| 5.5 | prediction | VEP SIFT/PolyPhen/AlphaMissense | Integrated |
| 6.0 | annotation | SnpEff annotation | Integrated |
| 6.1 | annotation evidence | dbSNP retrieval (online + local tabix) | Integrated |
| 6.2 | annotation evidence | ClinVar retrieval (online + local tabix) | Integrated |
| 6.3 | annotation evidence | gnomAD retrieval (online + persisted endpoint) | Integrated |
| 6.4, 6.5 | annotation evidence policy/completeness | Run-scoped stop/continue behavior + explicit completeness signaling | Integrated |
| 7.1 | annotation preflight | Evidence-mode detection + run telemetry persistence | Integrated |
| 7.2 | annotation evidence | Offline local source query hardening, provenance surfacing, and diagnostics guidance | Integrated |

---

## 1) Parser (VCF -> SNV records)

Story keys: `2-2`, `2-3`  
Primary code: `src/pipeline/parser_stage.py`, `src/vcf_parser.py`

### Input
- Uploaded VCF path from `run_inputs.upload_path`
- Max decompressed size (`SP_MAX_VCF_DECOMPRESSED_BYTES`)

### Processing format
- Reads VCF records, validates format, keeps SNVs only.
- Persists one row per SNV in `run_variants`.

### Output structure
- DB (`run_variants`):
  - `variant_id`, `run_id`, `chrom`, `pos`, `ref`, `alt`, `source_line`, `created_at`
- Stage stats include parser counters (accepted/rejected, duplicates, etc.).

### Failure behavior
- Stage error codes include parse/validation failures (for example `VCF_PARSE_FAILED`) with safe details.

---

## 2) Pre-Annotation (local-only context)

Story key: `4-1`  
Primary code: `src/pipeline/pre_annotation_stage.py`, `src/storage/pre_annotations.py`

### Input
- `run_variants` rows for the run/upload

### Processing format
- Deterministic local derivation only (no external API/tool):
  - `variant_key`, `base_change`, `substitution_class`, `ref_class`, `alt_class`

### Output structure
- DB (`run_pre_annotations`), public API (`GET /api/v1/runs/<run_id>/pre_annotations`)
```json
{
  "variant_id": "uuid",
  "variant_key": "1:69134:A>G",
  "base_change": "A>G",
  "substitution_class": "transition|transversion",
  "ref_class": "purine|pyrimidine",
  "alt_class": "purine|pyrimidine",
  "created_at": "ISO-8601"
}
```

---

## 3) Classification (VEP consequence routing)

Story key: `4-2`  
Primary code: `src/pipeline/classification_stage.py`, `src/storage/classifications.py`

### Input
- `run_variants` rows
- VEP runtime config (`SP_VEP_*`)

### Tool command format
- Builds minimal VCF and runs VEP JSON consequence extraction.
- Uses consequence terms to map category:
  - `missense`, `synonymous`, `nonsense`, `other`, else `unclassified`.
- Optional batching:
  - `SP_VEP_BATCH_SIZE` controls the per-batch variant count (set `0` to disable).
  - Batch artifacts are written as `classification.input.batchN.vcf` and `classification.vep.batchN.jsonl`.

### Output structure
- DB (`run_classifications`), public API (`GET /api/v1/runs/<run_id>/classifications`)
```json
{
  "variant_id": "uuid",
  "consequence_category": "missense|synonymous|nonsense|other|unclassified",
  "reason_code": "nullable",
  "reason_message": "nullable",
  "details": {},
  "created_at": "ISO-8601"
}
```

---

## 4) Prediction (VEP + SIFT/PolyPhen/AlphaMissense)

Story key: `5-5`  
Primary code: `src/pipeline/prediction_stage.py`, `src/storage/predictor_outputs.py`

### Input
- `run_variants` rows
- `run_classifications` category (routing gate)
- VEP config:
  - `SP_VEP_CMD`
  - `SP_VEP_SCRIPT_PATH`
  - `SP_VEP_CACHE_DIR`
  - `SP_VEP_PLUGIN_DIR`
  - `SP_VEP_ALPHAMISSENSE_FILE`
  - `SP_VEP_ASSEMBLY`
  - `SP_VEP_TIMEOUT_SECONDS`
  - `SP_VEP_EXTRA_ARGS`

### Tool command format
- Minimal VCF generated at `prediction.input.vcf`
- VEP invocation (conceptual):
```bash
<SP_VEP_CMD> <SP_VEP_SCRIPT_PATH?> \
  --input_file <prediction.input.vcf> \
  --output_file <prediction.vep.jsonl> \
  --format vcf --json --force_overwrite --offline --cache \
  --dir_cache <SP_VEP_CACHE_DIR> --assembly <SP_VEP_ASSEMBLY> \
  --sift b --polyphen b \
  --plugin AlphaMissense,file=<SP_VEP_ALPHAMISSENSE_FILE> \
  --dir_plugins <SP_VEP_PLUGIN_DIR> [--fasta <SP_VEP_FASTA_PATH>] [SP_VEP_EXTRA_ARGS...]
```

### Output structure
- Artifact: `instance/artifacts/<run_id>/prediction.vep.jsonl`
- DB (`run_predictor_outputs`), public API (`GET /api/v1/runs/<run_id>/predictor_outputs`)
```json
{
  "variant_id": "uuid",
  "predictor_key": "sift|polyphen2|alphamissense",
  "outcome": "computed|not_computed|not_applicable|error",
  "score": "float|null",
  "label": "string|null",
  "reason_code": "string|null",
  "reason_message": "string|null",
  "details": {},
  "created_at": "ISO-8601"
}
```

---

## 5) Annotation (SnpEff)

Story key: `6-0`  
Primary code: `src/pipeline/annotation_stage.py`

### Input
- `run_variants` rows
- SnpEff config:
  - `SP_SNPEFF_ENABLED`
  - `SP_SNPEFF_HOME` or `SP_SNPEFF_JAR_PATH`
  - `SP_SNPEFF_GENOME`
  - `SP_SNPEFF_DATA_DIR`
  - `SP_JAVA_CMD`
  - `SP_SNPEFF_JAVA_XMX`
  - `SP_SNPEFF_TIMEOUT_SECONDS`
  - `SP_SNPEFF_ARGS`

### Tool command format
- Minimal VCF generated, then:
```bash
<SP_JAVA_CMD> -Xmx<SP_SNPEFF_JAVA_XMX> -jar <snpEff.jar> \
  [ -config <SP_SNPEFF_CONFIG_PATH> ] \
  [ -dataDir <SP_SNPEFF_DATA_DIR> ] \
  <SP_SNPEFF_ARGS...> <SP_SNPEFF_GENOME> <input.vcf>
```

### Output structure
- Artifact: `instance/artifacts/<run_id>/snpeff.annotated.vcf`
- Stage stats in `run_stages.stats` (`annotation`)
- UI annotation table preview sourced from the artifact.

---

## 6) dbSNP Evidence (online + local retrieval)

Story key: `6-1` (updated with current API contract)  
Primary code: `src/pipeline/dbsnp_client.py`, `src/storage/dbsnp_evidence.py`

### Input
- Variant fields: `chrom`, `pos`, `ref`, `alt`
- Config:
  - `SP_EVIDENCE_MODE` (`online|offline|hybrid`)
  - `SP_DBSNP_ENABLED`
  - `SP_DBSNP_LOCAL_VCF_PATH` (local/offline mode path)
  - `SP_DBSNP_API_BASE_URL`
  - `SP_DBSNP_TIMEOUT_SECONDS`
  - `SP_DBSNP_RETRY_MAX_ATTEMPTS`
  - `SP_DBSNP_RETRY_BACKOFF_BASE_SECONDS`
  - `SP_DBSNP_RETRY_BACKOFF_MAX_SECONDS`
  - `SP_DBSNP_API_KEY` (optional)
  - `SP_DBSNP_ASSEMBLY` (default `GRCh38`)

### Request format (actual implementation)
Mode routing:
- `online`: uses API requests below.
- `offline`: uses local tabix lookup only.
- `hybrid`: local tabix lookup first; falls back to API on local lookup errors.

Local lookup format:
```bash
tabix <SP_DBSNP_LOCAL_VCF_PATH> <chrom>:<pos>-<pos>
```

Local chromosome matching behavior:
- Tries canonical aliases (`1`, `chr1`, `MT`, `chrM`, etc.).
- For dbSNP local VCF compatibility, also tries RefSeq accessions (for example `NC_000001.11`, `NC_012920.1`) because NCBI dbSNP releases may use RefSeq contig names.

Online API format:
1. Contextual normalization:
```http
GET {SP_DBSNP_API_BASE_URL}/vcf/{chrom}/{pos}/{ref}/{alt}/contextuals?assembly={SP_DBSNP_ASSEMBLY}[&api_key=...]
```
2. For each returned SPDI:
```http
GET {SP_DBSNP_API_BASE_URL}/spdi/{seq_id}:{position}:{deleted}:{inserted}/rsids[?api_key=...]
```

### Output structure
- DB (`run_dbsnp_evidence`), public API (`GET /api/v1/runs/<run_id>/dbsnp_evidence`)
```json
{
  "variant_id": "uuid",
  "source": "dbsnp",
  "outcome": "found|not_found|error",
  "rsid": "rs123|null",
  "reason_code": "string|null",
  "reason_message": "string|null",
  "details": {},
  "retrieved_at": "ISO-8601"
}
```
- Offline/hybrid provenance fields are included in `details` when local lookup is used:
  - `source_mode` (for example `offline_local`)
  - `local_vcf_path`, `record_region` (when available)

---

## 7) ClinVar Evidence (online + local retrieval)

Story key: `6-2`  
Primary code: `src/pipeline/clinvar_client.py`, `src/storage/clinvar_evidence.py`

### Input
- Variant fields: `chrom`, `pos`, `ref`, `alt`
- Config:
  - `SP_EVIDENCE_MODE` (`online|offline|hybrid`)
  - `SP_CLINVAR_ENABLED`
  - `SP_CLINVAR_LOCAL_VCF_PATH` (local/offline mode path)
  - `SP_CLINVAR_API_BASE_URL`
  - `SP_CLINVAR_TIMEOUT_SECONDS`
  - `SP_CLINVAR_RETRY_MAX_ATTEMPTS`
  - `SP_CLINVAR_RETRY_BACKOFF_BASE_SECONDS`
  - `SP_CLINVAR_RETRY_BACKOFF_MAX_SECONDS`
  - `SP_CLINVAR_API_KEY` (optional)

### Request format (actual implementation)
Mode routing:
- `online`: uses API requests below.
- `offline`: uses local tabix lookup only.
- `hybrid`: local tabix lookup first; falls back to API on local lookup errors.

Local lookup format:
```bash
tabix <SP_CLINVAR_LOCAL_VCF_PATH> <chrom>:<pos>-<pos>
```

Online API format:
1. Search:
```http
GET {SP_CLINVAR_API_BASE_URL}/esearch.fcgi?db=clinvar&retmode=json&retmax=1&term={chrom}[Chromosome] AND {pos}[Base Position for Assembly GRCh38] AND {ref}>{alt}[Canonical SPDI][&api_key=...]
```
2. Summary:
```http
GET {SP_CLINVAR_API_BASE_URL}/esummary.fcgi?db=clinvar&retmode=json&id={uid}[&api_key=...]
```

### Output structure
- DB (`run_clinvar_evidence`), public API (`GET /api/v1/runs/<run_id>/clinvar_evidence`)
```json
{
  "variant_id": "uuid",
  "source": "clinvar",
  "outcome": "found|not_found|error",
  "clinvar_id": "VCV...|null",
  "clinical_significance": "string|null",
  "reason_code": "string|null",
  "reason_message": "string|null",
  "details": {},
  "retrieved_at": "ISO-8601"
}
```
- Offline/hybrid provenance fields are included in `details` when local lookup is used:
  - `source_mode` (for example `offline_local`)
  - `local_vcf_path`, `record_region` (when available)

---

## 8) gnomAD Evidence (online retrieval)

Story key: `6-3` (implemented in code path)  
Primary code: `src/pipeline/gnomad_client.py`, `src/pipeline/annotation_stage.py`

### Input
- Variant fields: `chrom`, `pos`, `ref`, `alt`
- Config:
- `SP_EVIDENCE_PROFILE` (forced to `predictor_only`; evidence is missense-only)
  - `SP_EVIDENCE_MODE` (`online` project default)
  - `SP_GNOMAD_ENABLED`
  - `SP_GNOMAD_API_BASE_URL`
  - `SP_GNOMAD_DATASET_ID` (default `gnomad_r4`)
  - `SP_GNOMAD_REFERENCE_GENOME` (retained for config compatibility)
  - `SP_GNOMAD_TIMEOUT_SECONDS`
  - `SP_GNOMAD_RETRY_MAX_ATTEMPTS`
  - `SP_GNOMAD_RETRY_BACKOFF_BASE_SECONDS`
  - `SP_GNOMAD_RETRY_BACKOFF_MAX_SECONDS`
  - `SP_GNOMAD_MIN_REQUEST_INTERVAL_SECONDS`

### Request format (actual implementation)
Online GraphQL API format:
GraphQL POST to:
```http
POST {SP_GNOMAD_API_BASE_URL}
Content-Type: application/json
```
Payload:
```json
{
  "query": "query Variant($variantId: String!, $dataset: DatasetId!) { variant(variantId: $variantId, dataset: $dataset) { variantId rsid joint { ac an } genome { af ac an } exome { af ac an } } }",
  "variables": {
    "variantId": "1-69134-A-G",
    "dataset": "gnomad_r4"
  }
}
```
Client behavior:
- Tries `variantId` candidates (`1-...`, `chr1-...`, MT variants).
- Applies request pacing + `Retry-After` aware retry on `429`.
- Classifies GraphQL body errors into:
  - `GRAPHQL_SCHEMA_ERROR` for invalid field/dataset/query-contract errors (non-retryable)
  - `GRAPHQL_ERROR` for other GraphQL failures (retryable only when transient)
- Accepts partial responses when `data.variant` is present even if `errors[]` exists.

### Output structure
- DB (`run_gnomad_evidence`), public API (`GET /api/v1/runs/<run_id>/gnomad_evidence`)
```json
{
  "variant_id": "uuid",
  "source": "gnomad",
  "outcome": "found|not_found|error",
  "gnomad_variant_id": "1-69134-A-G|null",
  "global_af": 0.00123,
  "reason_code": "string|null",
  "reason_message": "string|null",
  "details": {},
  "retrieved_at": "ISO-8601"
}
```
- Offline/hybrid provenance fields are included in `details` when local lookup is used:
  - `source_mode` (for example `offline_local`)
  - `local_vcf_path`, `record_region`, `af_source` (when available)
- Annotation stage stats also include:
  - `gnomad_found`, `gnomad_not_found`, `gnomad_errors`
  - `gnomad_error_reason_counts`
  - `gnomad_error_http_status_counts`
  - `gnomad_retry_attempts`
  - `gnomad_variants_eligible`
  - `gnomad_skipped_out_of_scope`

Profile scope note:
- Evidence annotation is enforced as missense-only. Profile settings are ignored.
- Only `missense` variants are eligible for evidence queries.
- Stage stats surface the same eligible/skipped counters for dbSNP and ClinVar:
  - `dbsnp_variants_eligible`, `dbsnp_skipped_out_of_scope`
  - `clinvar_variants_eligible`, `clinvar_skipped_out_of_scope`

---

## 9) Annotation Evidence Failure Policy And Completeness (run-scoped)

Story key: `6-4`, `6-5`  
Primary code: `src/storage/runs.py`, `src/app.py`, `src/pipeline/annotation_stage.py`, `src/static/upload_controls.js`, `src/static/run_controls.js`, `src/static/results_controls.js`

### Input
- Run-scoped policy value:
  - `stop`
  - `continue`
- UI setup control: Start panel radio group (`annotation-evidence-policy`)
- API payload field: `annotation_evidence_policy`

### Request/API format
1. Create run with policy:
```http
POST /api/v1/runs
Content-Type: application/json

{ "annotation_evidence_policy": "continue" }
```

2. Update run policy before start/retry:
```http
POST /api/v1/runs/{run_id}/settings
Content-Type: application/json

{ "annotation_evidence_policy": "stop" }
```

### Runtime behavior
- `stop`:
  - annotation stage fails on evidence retrieval failure
  - error details include `failed_source`, computed `missing_outputs`, `annotation_evidence_policy`
- `continue`:
  - annotation stage succeeds with warning/error counters in stats
  - stats include `annotation_evidence_policy`, `fail_on_evidence_error`, `evidence_failed_sources`

### Completeness behavior
- Annotation stage computes explicit completeness markers:
  - `annotation_evidence_completeness`: `complete|partial|unavailable`
  - `evidence_source_completeness`: map for `dbsnp|clinvar|gnomad`
  - `evidence_source_completeness_reason`: per-source reason codes
  - `evidence_complete_sources|evidence_partial_sources|evidence_unavailable_sources`
- Reporting stage carries forward completeness summary so final-result state cannot silently imply full evidence completeness.

### Evidence mode detector behavior (Story 7.1, Story 7.2)
- Input signals:
  - Requested mode from `SP_EVIDENCE_MODE` (`online|offline|hybrid`)
  - Online availability from short bounded connectivity probes
  - Offline configured map from local VCF path presence (`dbsnp|clinvar|gnomad`)
  - Offline available map from local source readiness + source enabled (`dbsnp|clinvar|gnomad`)
- Decision output (persisted on `runs`):
  - `evidence_mode_requested`
  - `evidence_mode_effective`
  - `evidence_online_available`
  - `evidence_offline_sources_configured` (`dbsnp|clinvar|gnomad`)
  - `evidence_mode_decision_reason`
  - `evidence_mode_detected_at`
- Additional annotation-stage diagnostics (not run-level persisted fields):
  - `evidence_offline_sources_available` (`dbsnp|clinvar|gnomad`)
  - `evidence_offline_sources_unavailable_reason` (`dbsnp|clinvar|gnomad`)
- Effective mode rules:
  - Requested `online`: use `online`, fallback to `offline` if online unavailable and offline configured
  - Requested `offline`: use `offline`, fallback to `online` if offline unavailable and online available
  - Requested `hybrid`: use `hybrid` only when both paths are available, else degrade to available single path
  - If neither path is available, keep requested mode and persist reason, then fail annotation via `EVIDENCE_SOURCES_UNAVAILABLE` (Story 7.3 behavior).

### Backward-compatible defaults
- New default env: `SP_ANNOTATION_EVIDENCE_POLICY_DEFAULT` (`continue|stop`)
- Legacy fallback: `SP_ANNOTATION_FAIL_ON_EVIDENCE_ERROR` (`0 => continue`, `1 => stop`) only when new default is unset.

### Output structure
- Runs API payloads now include:
```json
{
  "run_id": "uuid",
  "status": "queued|running|...",
  "reference_build": "GRCh38",
  "annotation_evidence_policy": "continue|stop",
  "evidence_mode_requested": "online|offline|hybrid",
  "evidence_mode_effective": "online|offline|hybrid|null",
  "evidence_online_available": true,
  "evidence_offline_sources_configured": {
    "dbsnp": false,
    "clinvar": false,
    "gnomad": false
  },
  "evidence_mode_decision_reason": "string|null",
  "evidence_mode_detected_at": "ISO-8601|null"
}
```
- Annotation stage stats include per-source readiness maps:
  - `evidence_offline_sources_available`
  - `evidence_offline_sources_unavailable_reason`
- Annotation stage failure details (stop mode) include:
```json
{
  "failed_source": "dbsnp|clinvar|gnomad",
  "missing_outputs": ["computed by failure point/order"],
  "annotation_evidence_policy": "stop",
  "annotation_evidence_completeness": "partial|unavailable",
  "evidence_source_completeness": {
    "dbsnp": "complete|partial|unavailable",
    "clinvar": "complete|partial|unavailable",
    "gnomad": "complete|partial|unavailable"
  }
}
```

### Strict no-valid-source block contract (Story 7.3)
- Trigger condition:
  - enabled evidence sources exist, and
  - online is unavailable, and
  - all required offline sources are unavailable for those enabled sources.
- Annotation stage fails with:
  - `error.code = "EVIDENCE_SOURCES_UNAVAILABLE"`
  - `error.message = "Neither online nor offline evidence sources are available for annotation."`
- Error details include:
```json
{
  "requested_mode": "online|offline|hybrid",
  "effective_mode": "online|offline|hybrid",
  "decision_reason": "requested_*_no_valid_source",
  "online_available": false,
  "offline_sources_configured": {"dbsnp": false, "clinvar": false, "gnomad": false},
  "offline_sources_available": {"dbsnp": false, "clinvar": false, "gnomad": false},
  "offline_sources_unavailable_reason": {"dbsnp": "", "clinvar": "", "gnomad": ""},
  "enabled_sources": {"dbsnp": true, "clinvar": true, "gnomad": true},
  "required_sources": ["dbsnp", "clinvar", "gnomad"],
  "missing_sources": ["dbsnp", "clinvar", "gnomad"],
  "missing_source_env_vars": [
    "SP_DBSNP_LOCAL_VCF_PATH",
    "SP_CLINVAR_LOCAL_VCF_PATH",
    "SP_GNOMAD_LOCAL_VCF_PATH"
  ],
  "missing_outputs": ["dbsnp", "clinvar", "gnomad", "reporting"],
  "blocked_outputs": ["annotation", "reporting"],
  "failed_source": "all_evidence_sources_unavailable",
  "annotation_evidence_policy": "continue|stop",
  "annotation_evidence_completeness": "unavailable",
  "hint": "Restore connectivity or configure indexed local VCF paths and retry."
}
```
- Reporting and downstream signaling behavior:
  - reporting stage remains `queued` for that upload
  - annotation-dependent result summaries must show blocked outputs and missing sources
  - pipeline does not produce a misleading successful completion for that upload.

---

## 10) API Exposure Summary (latest-upload gated)

These endpoints return data only when the corresponding stage succeeded for the latest upload:
- `GET /api/v1/runs/<run_id>/pre_annotations`
- `GET /api/v1/runs/<run_id>/classifications`
- `GET /api/v1/runs/<run_id>/predictor_outputs`
- `GET /api/v1/runs/<run_id>/dbsnp_evidence`
- `GET /api/v1/runs/<run_id>/clinvar_evidence`
- `GET /api/v1/runs/<run_id>/gnomad_evidence`

If stage/upload mismatch exists, endpoints return `ok: true` with empty arrays for safety.
